#include <stdio.h>

int main()
{
    int i = 10; // Declare and initialize 'i' with 10
    int j = i;
    int a = 2, b = 3, c = 4, d = 5;
    // %d is called forma  t specifier
    // %d is for int, %f is for float and %c is for char
    printf("The vaule of i is %d and value of j is %d\n", i, j);
    printf("The vaule of a is %d and value of b is %dn\n", a, b);
    printf("The vaule of c is %d and value of d is %d", c, d);
    return 0;
}
