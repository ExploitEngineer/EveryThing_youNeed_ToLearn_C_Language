// typedef is Used to add an additional name to a type. Useful when working with more complex types, making them more human readable.

typedef unsigned char byte;

int main(void)
{
    byte b1 = 'c';
    return 0;
}
