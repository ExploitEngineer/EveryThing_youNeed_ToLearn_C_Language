#include <stdio.h>

int main()
{
    int age = 5;
    if (age > 10)
    {
        printf("We are inside if statement\n");
    }
    else{
        printf("Your age is not greater than 10");
    }
    return 0;
}
