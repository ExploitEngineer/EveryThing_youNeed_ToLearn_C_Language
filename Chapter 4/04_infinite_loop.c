#include <stdio.h>

int main()
{
    int i = 0;
    while (2 < 10)
    {
        printf("The value of i is %d\n", i);
        i++;
    }

    while (i < 10)
    {
        printf("Don't run this code your pc will get into death mode");
    }
    return 0;
}
