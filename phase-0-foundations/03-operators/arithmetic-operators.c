#include <stdio.h>

int main(void)
{
    // arithmetic operators = + - * / % ++ --

    int x = 2;
    int y = 3;
    int z = 0;

    // z = x + y;
    // z = x - y;
    // z = x * y;
    // z = x / y;
    // z = x % y;
    // z = x % y;

    // x++;
    // x--;

    x += 5;
    x -= 5;
    x *= 5;
    x /= 5;

    printf("%d", x);

    return 0;
}
